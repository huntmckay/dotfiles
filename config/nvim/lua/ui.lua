-- === Theme Setup ===

-- === Lualine ===
require("lualine").setup({
  options = {
    theme = "auto", -- auto-detects from colorscheme
    section_separators = "",
    component_separators = "|",
  },
})
